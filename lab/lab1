/*
Name: Rishita
Date: 29/01/25
Purpose: To find the largest among three numbers using nested if statements.
*/
#include<stdio.h>
int main(){
  int num1, num2, num3;
  printf("\nEnter first number: ");
  scanf("%d", &num1);
  printf("\nEnter second number: ");
  scanf("%d", &num2);
  printf("\nEnter third number: ");
  scanf("%d", &num3);
  
  if (num1>num2){
    if (num1>num3){
      printf("\n%d is the largest number.", num1);
    }
    else{
      printf("\n%d is the largest number.", num3);
    }
  }
    
  else if (num2>num1){
    if(num2>num3){
      printf("\n%d is the largest number", num2);
    }
    else{
    printf("\n%d is the larges number", num3);
    }
  }
  
  else{
    printf("\nAll numbers are equal.");
  }
  
  return 0;
}
